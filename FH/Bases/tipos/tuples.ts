(()=>{
    // Esto es un arreglo de diferentes tipos:
    const heros: (string | number)[] = ['Dr Strange', 100];

    // Esto es una TUPLA:
    const heros2: [string, number] = ['Dr Strange', 100];

})()